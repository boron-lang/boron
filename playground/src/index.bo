mod vec;
import * from vec;

extern "C" func puts(text: *const u8);

pub enum Option<T> {
    Some(T),
    None,
}

func main() {
    var value = Option::Some("hello");
    var x = Vec2 { .x = 10.0, .y = 20.0 };
}
