extern "C" func puts(text: *const u8);

pub struct Vec2 {
    x: f32,
    y: f32,

    func new(x: f32, y: f32) -> Vec2 {
        Vec2 { .x, .y }
    }

    pub func sum(self) -> f32 {
        self.x + self.y
    }
}

func main() {
    var val = Vec2::new(15.0, 10.0);

    var sum = val.sum();

    if comptime os!() == "windows" {

    }
}
