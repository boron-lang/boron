func identity<T>(value: T) -> T {
    value
}

//comptime func square(a: i32) -> i32 {
//    return a * a;
//}

//func x(t: [i32; 10 + 10]) {}

struct Vec2<T> {
    x: T,
    y: T
}

pub func test() {
    var value = Vec2 { .x = 10.0, .y = 15.0 };
}
