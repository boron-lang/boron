func identity<T>(value: T) -> T {
    value
}

//comptime func square(a: i32) -> i32 {
//    return a * a;
//}

mod vec;

import vec from vec;

func main() {
    var value = vec::Vec2 { .x = 10.0, .y = "15.0" };
}