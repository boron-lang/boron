func identity<T>(value: T) -> T {
    value
}

//comptime func square(a: i32) -> i32 {
//    return a * a;
//}

//func x(t: [i32; 10 + 10]) {}

const ARRAY_LEN: i32 = 10;

pub func test() {
    var value = identity(10) + "dwas";
}
