func identity<T>(value: T) -> T {
    value
}

comptime func square(a: i32) -> i32 {
    return a * a;
}

pub func test() {
    var a = identity(10);
    var b = square!(20);
    var c = sizeOf!(i32);
}
