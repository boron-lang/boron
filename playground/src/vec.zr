pub struct Vec2<T> {
    x: T,
    y: T,

    func new(x: T, y: T) -> Vec2<T> {
        Vec2 { .x, .y }
    }
}

func identity<T>(value: T) -> T {
    return T;
}

func main() {
    var test = identity(Vec2 { .x = 10.0, .y = 15.0 });
    //var x = Vec2 { .x = 10, .y = 15 };
}