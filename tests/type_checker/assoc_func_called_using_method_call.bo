pub enum Option<T> {
    Some(T),
    None,

    func test() {
    }
}

func main() {
    //#error: cannot call associated function `Option<{}>::test` using a method call
    var value = Option::Some("hello");

    value.test();
}
