pub struct Vec2<T> {
    x: T,
    y: T
}

struct Hello {
    value: *const u8
}

func main() {
    //#error: attempted to initialize field with `*const u8`, but expected `f64`
    var vec = Vec2 { .x = 10.0, .y = "Food Wars" };

    //#error: attempted to initialize field with `i32`, but expected `*const u8`
    var hello = Hello { .value = 10 };
}