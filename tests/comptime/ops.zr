//#error: cannot apply binary operator `+` to `integer` and `*const u8`
const X: i32 = 10 + "10";
//#error: cannot apply unary operator `~` to `*const u8`
const Y: i32 = ~"10";

func main() {
    var val = X + Y;
}